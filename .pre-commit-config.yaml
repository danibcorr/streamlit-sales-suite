repos:
  - repo: local
    hooks:
      - id: local-check
        name: Makefile Validation
        entry: make pre-commit
        language: system
        pass_filenames: false
        always_run: true
        verbose: true
